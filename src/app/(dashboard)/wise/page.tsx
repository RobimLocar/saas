import { Sparkles } from "lucide-react";
import { PagePlaceholder } from "@/components/shared/page-placeholder";

export default function WisePage() {
  return (
    <PagePlaceholder
      icon={Sparkles}
      title="Wise"
      description='O Wise já vive dentro do Studio (botão "Wise enhance"). A página dedicada vem em breve.'
    />
  );
}
